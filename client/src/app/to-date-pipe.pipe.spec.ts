import { ToDatePipePipe } from './to-date-pipe.pipe';

describe('ToDatePipePipe', () => {
  it('create an instance', () => {
    const pipe = new ToDatePipePipe();
    expect(pipe).toBeTruthy();
  });
});
